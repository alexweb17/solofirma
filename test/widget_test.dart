import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Empty test', (WidgetTester tester) async {
    // Este es un test vacío que no hace nada,
    // pero evita que el archivo dé errores.
  });
}